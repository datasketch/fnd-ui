import React from 'react'
import { CardColumnProps } from './CardColumn.types'
import '../../sass/main.scss'
const CardElement: React.FC<CardColumnProps> = ({ title, titleAlign, childrenAlign, image, imageBorderRadius, anchorLabel, children, href, alt }) => {
  return (
    <div className='fnd-ui-CardElement-container'>

    </div>
  )
}

export default CardElement
